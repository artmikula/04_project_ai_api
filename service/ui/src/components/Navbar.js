import React from "react";
import { useNavigate } from "react-router-dom";

function Navbar({ setUser, setUserAdded, login, logout }) {
  // let navigate = useNavigate();

  return <div>nav</div>;
}

export default Navbar;
